{
  "name": "nordisk-mobilvask",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "assemble": "node scripts/assemble.mjs",
    "check": "npm run assemble && node --check src/worker.js",
    "dev": "npm run assemble && wrangler dev",
    "deploy": "npm run assemble && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
